class TypeCheckAbort
  super TypeCheckError
  metaclass SimpleConcreteClass(TypeCheckAbort)

private methods

  _init :Self {
    super._init,
    setError("Type checking aborted (user/too large recursion). Maybe you need to increase tycoon.tl.typeChecker.maxDepth"),
    self
  }
;
