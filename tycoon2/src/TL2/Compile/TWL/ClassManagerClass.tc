class ClassManagerClass
super ConcreteClass(ClassManager)
metaclass MetaClass
public methods

new(root :Root, pool :Pool, typeManager :TypeManager) :ClassManager
{
  let instance = _new,
  instance._root := root,
  instance._pool := pool,
  instance._typeManager := typeManager,
  instance._init
}
;
