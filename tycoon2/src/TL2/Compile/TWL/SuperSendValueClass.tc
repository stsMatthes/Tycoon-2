class SuperSendValueClass
super ConcreteClass(SuperSendValue)
metaclass MetaClass
public methods

new( pos :SourcePos, 
     receiver :Value, selector :Symbol, args :List(Value)) :SuperSendValue
{
  let instance = _new,
  instance.pos := pos,
  instance.receiver := receiver,
  instance.selector := selector,
  instance.args := args,
  instance._init
}
;
