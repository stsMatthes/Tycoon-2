class SubTypeTraceClass
  super ConcreteClass(SubTypeTrace)
  metaclass MetaClass

public methods

  new(small :Type, big :Type) :SubTypeTrace
  {
    let instance = _new,
    instance._initTypes(small, big),
    instance
  }

;
