class TLClass
super ConcreteClass(TL)
metaclass MetaClass
public methods

new(root :Root, pool :Pool, vmInterface :VMInterface) :TL
{
  let instance = _new,
  instance.root := root,
  instance.pool := pool,
  instance.vmInterface := vmInterface,
  instance._init
}
;
