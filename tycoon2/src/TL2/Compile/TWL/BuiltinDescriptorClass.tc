class BuiltinDescriptorClass
super ConcreteClass(BuiltinDescriptor)
metaclass MetaClass
public methods
new(n :Int,
    className :String, selector :String, arity :Int) :BuiltinDescriptor
{
  let instance = _new,
  instance.n := n,
  instance.className := className,
  instance.selector := selector,
  instance.arity := arity,
  instance._init
}
;
