class ScoperClass
super ConcreteClass(Scoper)
metaclass MetaClass
public methods

new(currentClassSig :TypeIde,
    errorLog :ErrorLog,
    typeManager :TypeManager) :Scoper
{
  let instance = _new,
  instance.currentClassSig := currentClassSig,
  instance.errorLog := errorLog,
  instance.typeManager := typeManager,
  instance._init
}
;
