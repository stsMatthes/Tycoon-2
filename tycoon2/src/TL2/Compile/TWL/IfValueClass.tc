class IfValueClass
super ConcreteClass(IfValue)
metaclass MetaClass
public methods
new1(pos :SourcePos, condition :Value, trueBranch :Value, falseBranch :Value) :IfValue
{
  let instance = _new,
  instance.pos := pos,
  instance.condition := condition,
  instance.trueBranch := trueBranch,
  instance.falseBranch := falseBranch,
  instance._init
}
;
