class IdeValueClass
super ConcreteClass(IdeValue)
metaclass MetaClass
public methods

new(ide :ValueIde) :IdeValue
{
  let instance = _new,
  instance.pos := ide.pos,
  instance.ide := ide,
  instance
}
;
