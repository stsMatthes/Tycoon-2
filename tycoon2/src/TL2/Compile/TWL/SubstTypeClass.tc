class SubstTypeClass
super ConcreteClass(SubstType)
metaclass MetaClass
public methods
new(type :Type, subst :Subst) :SubstType
{
  let instance = _new,
  instance.type := type,
  instance.substi := subst,
  instance
}
;
