class MethodManagerClass
super ConcreteClass(MethodManager)
metaclass MetaClass
public methods
new(method :CompiledMethod, bindings :Bindings, compiler :Compiler) :MethodManager
{
  let instance = _new,
  instance.method := method,
  instance.bindings := bindings,
  instance.compiler := compiler,
  instance._init
}
;
