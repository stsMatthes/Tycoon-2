class OperTypeClass
super ConcreteClass(OperType)
metaclass MetaClass
public methods
new(domain :List(TypeIde), range :Type) :OperType
{
  let instance = _new,
  instance.domain := domain,
  instance.range := range,
  instance
}
;
