class TypeCheckerClass
super ConcreteClass(TypeChecker)
metaclass MetaClass
public methods

new(classManager :ClassManager, pool :Pool) :TypeChecker
{
  let instance = _new,
  instance.classManager := classManager,
  instance.pool := pool,
  instance._init
}
;
