class ConsSubstClass
super ConcreteClass(ConsSubst)
metaclass MetaClass
public methods
new(ide :Ide, type :Type, tail :Subst) :ConsSubst
{
  let instance = _new,
  instance.ide := ide,
  instance.type := type,
  instance.tail := tail,
  instance
}
;
