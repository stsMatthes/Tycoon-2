class ConcreteEncoderClass(Instance <: Encoder)
super ConcreteClass(Instance)
metaclass MetaClass

public methods

new(i :Input) :Instance
{
  let instance = _new,
  instance._init1(i),
  instance
}

;
